# Google Search Console verification token (meta-tag method).
# Get it from https://search.google.com/search-console → Add property → HTML tag,
# copy ONLY the content="..." value. Leave empty until then — the meta tag is
# only rendered when this is set. Never commit a real token in code.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
